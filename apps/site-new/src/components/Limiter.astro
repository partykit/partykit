---

---

<div class="limiter"><slot /></div>

<style>
  .limiter {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 0 1rem;
  }

  @media (min-width: 760px) {
    .limiter {
      grid-template-columns: repeat(10, 1fr);
    }
  }

  .limiter {
    gap: 1rem;
    overflow: hidden;
    -webkit-mask-image: linear-gradient(
      to bottom,
      #000 calc(100% - 8rem),
      transparent calc(100% - 1px)
    );
  }
</style>
