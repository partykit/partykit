---
interface Props {
  variant?: null | "nested";
}

const { variant = null } = Astro.props;

---

<div class={variant}><slot /></div>

<style>
  div {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1rem;
    padding: var(--padding);
  }
  
  div.nested {
    padding: 0;
  }

  @media (min-width: 760px) {
    div {
      grid-template-columns: repeat(10, 1fr);
    }
  }
</style>
